{
  "name": "shell",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor thin shell — wraps the deployed portal in a native WebView (iOS + Android).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"shell: config-only package — typecheck is the lint gate; ios/android are generated\"",
    "test": "echo \"shell: no unit tests — native verification is on-device (see README)\"",
    "build": "echo \"shell: web assets live in the deployed portal; native builds via cap:open or Xcode/Gradle\"",
    "cap:add": "cap add ios && cap add android",
    "cap:add:ios": "cap add ios",
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync",
    "cap:open:ios": "cap open ios",
    "cap:open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.5.1",
    "@capacitor/ios": "^8.5.1",
    "@capacitor/preferences": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1",
    "@types/node": "^24.13.4",
    "typescript": "^7"
  }
}

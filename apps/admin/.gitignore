node_modules/
.next/
out/
build/
dist/

.env
.env.local
.env*.local

.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.vercel
.idea/
.vscode/
*.tsbuildinfo
next-env.d.ts

# Test output
coverage/
/test-results/
playwright-report/

# Playwright storageState — contains live session cookies; never commit
e2e/support/.auth/

# Pre-push gate marker (scripts/pre-push-gate.mjs --stamp) — per-machine, never committed
.claude/pre-push-ok.json

# drizzle-kit generate's own scratch snapshot folder, if this app ever gains
# a narrow identity-style bootstrap config mirroring the ancestor kit's
# drizzle.identity.config.ts — disposable, file-based, never the live DB.
.drizzle-identity-local/

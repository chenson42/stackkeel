---
title: AI-Assisted Development Workflow for Claude, Codex, and Copilot
description: "Stackkeel's six-phase development pipeline for AI coding assistants: analyst, architect, tech lead, implementer, and QA roles, work-log discipline, evidence classes, and quality gates enforced by git hooks and CI."
---

import { Image } from "astro:assets";
import worklog from "../../assets/screenshots/worklog.png";

The kit ships an opinionated AI-assisted development workflow, described tool-neutrally in `AGENTS.md` so any assistant can execute it.

<Image src={worklog} alt="A real work-log from the kit's own repository: the six-phase status table with owners, verdicts, and dates rendered on GitHub" />

## The six phases

1. **Functional refinement** (analyst) — user verbs, flows, auth gates, gaps, adversarial pass
2. **Architectural review** (architect) — placement, server/client split, invariant compliance
3. **Technical design** (tech-lead) — design doc: contracts, data model, implementation order
4. **Implementation** (database-admin → api-developer → ux-developer, or full-stack for small work; mobile-developer owns shell + mobile)
5. **Test verification** (qa) — unit + e2e + auth audit; PASS / FAIL / BLOCKED
6. **Shipped-vs-intent** (analyst) — the SHIP IT verdict

Every feature gets a work-log file *before* code (`docs/work-log/`), classified Trivial / Polish / Feature / Spike. Loop-backs return to the earliest phase where the failure originated.

## Which model runs which role

In Claude Code each role is a subagent under `.claude/agents/`, and each agent pins a model in its frontmatter. The tier follows the cost of a wrong answer, not the size of the task ([DECISION-007](https://github.com/chenson42/stackkeel/blob/main/docs/decisions.md)):

| Tier | Agents | Why |
|---|---|---|
| **fable** (most capable) | analyst, qa | The two verdict gates whose documented failure mode is rubber-stamping. Phase 1 shapes every downstream phase; Phase 5's derive-then-diff and Phase 6's SHIP IT are the last places a bad claim can be caught. Both are read-only and run once per feature, so the premium buys judgment, not token volume. |
| **opus** | architect, tech-lead, api-developer, database-admin, mobile-developer | Design rulings and the implementers whose mistakes are expensive to reverse: invariant compliance and code review, the API contract and data model, auth/permission/audit server code, migrations, and native Swift/Kotlin with thin test coverage. |
| **sonnet** | ux-developer, full-stack-developer, deployment-engineer | Contract-driven, bounded, or command-and-read work that qa and the UX audit checklist re-verify afterward. Highest token volume per feature, lowest marginal value from a stronger model. |

Rejected alternatives: all-sonnet (puts the weakest judgment on the phases the evidence rules exist to harden), all-fable (roughly twice the per-token cost of opus with no measured gain on contract-driven UI work, and its longer turns slow the high-volume implementers), haiku for any role (no roster agent is a bulk-search role), and `inherit` (phase quality would drift with whichever model the operator launched the session on). The per-invocation model override remains the escape hatch in both directions. Other assistants play the roles sequentially with whatever model they run; the phase contract is what's binding, not the tiering.

## Evidence discipline

A completed phase carries **evidence, not checkmarks**: claims are either *Verified* (the implementation was read, file and line range cited) or *Indicative* (everything else — grep hits, filenames, comments, prior docs). Indicative can motivate a claim and can never support one. Each phase re-derives its predecessor's load-bearing claims *before* reading the claimed answers, then diffs — trust is earned per handoff, not assumed. Work-log entries include a "What was NOT verified" section; honesty about the gaps is what makes the ledger useful.

Two honest caveats, stated here because "enforced" invites over-reading. The work-log gate verifies that citations are **present**; no mechanical check can verify they are true — the derive-then-diff entry check is what does that, and it is a discipline, not a script. And a claim that asserts its own verification ("confirmed by direct search") is still Indicative: the assertion is not the verification.

Work-logs should be sized to the work. A small feature's log runs about a hundred lines — see the [worked example](https://github.com/chenson42/stackkeel/blob/main/docs/work-log/_example-small-feature.md) shipped in the kit — while the kit's own multi-thousand-line bootstrap logs documented whole construction phases and are the ceiling, not the norm.

## Enforcement stack

| Layer | What enforces it |
|---|---|
| Commit grammar + `Caught-By`/`Discovered-In` trailers on fixes | `commit-msg` git hook + CI range validator |
| No code before the work-log | `pre-commit` git hook + Claude PreToolUse hook |
| Pre-push verification (typecheck, lint, tests, tripwires) | `pre-push` git hook running `pnpm kit:verify` |
| Instruction freshness, symbol drift, secrets, SQL date safety, brand scope | `pnpm check` tripwire suite, run in CI |
| Escape-rate telemetry | `pnpm stats:escape` feeds the retrospective |

Assistant-specific hooks (`.claude/settings.json`) give fast in-session feedback, but the git hooks and CI are the binding layer. The workflow holds no matter which tool wrote the code.
